[tool.poetry]
name = "time-tracker"
version = "0.1.0"
description = ""
authors = ["delbik <m4rk.brag1n@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.8"
textual = "^0.1.18"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
